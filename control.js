window.onerror = function(msg, src, line, col, err) {
    console.log("REAL ERROR:");
    console.log({ msg, src, line, col, err });
};

console.log("SCRIPT START");

setTimeout(() => {
    console.log("SCRIPT STILL ALIVE");
}, 1000);    <h2>Unlock Premium Funds</h2>
